# Core Django dependencies
asgiref==3.8.1
Django==4.2.7
django-cors-headers==4.3.1
djangorestframework==3.14.0
djangorestframework_simplejwt==5.5.0

# Database (using PostgreSQL for production, SQLite fallback)
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Image processing (Vercel-compatible versions)
Pillow>=10.0.0
numpy>=1.24.0

# OCR libraries (lightweight versions for serverless)
pytesseract==0.3.10
pyzbar==0.1.9

# Computer vision (CPU-only version for serverless)
opencv-python-headless>=4.8.0

# Utilities
python-dotenv==1.0.1
python-decouple==3.8
pytz==2025.2
sqlparse==0.5.3
tzdata==2025.2

# Serverless deployment
whitenoise==6.6.0

# Optional: Lightweight OCR alternatives (comment out heavy ML libs for now)
# easyocr>=1.7.0
# transformers>=4.36.0
# torch>=2.0.0
# torchvision>=0.15.0
# datasets>=2.14.0
# rapidfuzz>=3.5.0
# spacy>=3.7.0
# textdistance>=4.6.0
# scikit-image>=0.22.0
# albumentations>=1.3.0
